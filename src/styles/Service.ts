import { styled } from '../../stitches.config';

export const Service = styled('section', {
  alignItems: 'center',
  justifyContent: 'center',
  flexDirection: 'column',
  padding: '8rem 4rem 0px',
  background: 'url(/serviceBg.png) center',
  backgroundSize: 'cover',
});
