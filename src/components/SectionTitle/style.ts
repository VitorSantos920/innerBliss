import { styled } from '../../../stitches.config';

export const SectionTitleContainer = styled('div', {
  alignItems: 'center',
  gap: '.5rem',
  marginBottom: '1rem',

  h2: {
    color: '$lightGreen',
    fontFamily: '$poppins',
    fontWeight: 400,
    fontSize: '1.125rem',
  },
});
